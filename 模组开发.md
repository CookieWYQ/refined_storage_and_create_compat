# 模组需求汇总：精致存储 & 机械动力：兼容与改善

## 1. 模组基础信息
- 中文名：精致存储&机械动力：兼容与改善
- 英文名：refined_storage_and_create_compat
- 模组ID：rs_create_compat
- 游戏版本：Minecraft 1.21.1
- 模组平台：NeoForge
- 语言文件：存放于 `assets/rs_create_compat/lang/` 目录下，为 `.json` 格式（如 `en_us.json`、`zh_cn.json`）。
- 配置文件：存放于 `config/` 目录下，为 `.toml` 格式（所有数值/开关类配置均在此，GUI调整项除外）。

---

## 2. 方块：蓝图加农炮装填器（基础版）
- **命名ID（拟定）**：`rs_create_compat:schematic_loader`
- **核心功能**：
  - 自动从精致存储（RS）网络中获取蓝图加农炮内“已部署蓝图”所需的所有资源。
  - 自身可被蓝图加农炮识别为一个合法容器。
- **存储容量**：基础容量 = 1个大箱子（54格）。
- **插件槽位**：共6个，可安装：
  - 速度升级（加快从RS网络**抽取物品至装填器**的速度，与加农炮打印速度无关）。
  - 自动合成升级（允许RS网络自动合成缺失资源）。
  - （注：**容量升级插件不包含**，未来仅作可选项，当前靠多装填器解决容量问题）。
- **蓝图槽位逻辑**：
  - 当**没有**紧贴的蓝图加农炮时：允许手动放入一张已部署蓝图，点击按钮获取全部资源。
  - 当**有**紧贴的蓝图加农炮时：该手动蓝图槽位**自动禁用**，转而读取加农炮内的蓝图。
- **多装填器协同**：
  - 资源需求超过1个大箱子时，可放置多个装填器。它们通过**互相紧贴**连成一片（多方块结构集群），自动分配资源获取任务，**不会重复获取**。
  - **严格禁止**使用任何线缆连接装填器与装填器，仅允许物理紧贴连接。
  - 总空间不足时，游戏需提示玩家空间不足，并告知还需放置几个装填器。
- **基础版开关（GUI中可配置）**：
  - 是否自动开始打印（触发加农炮）。
  - 是否自动部署蓝图。
  - 是否自动回收蓝图（打印完成后回收空白蓝图）。
  - 是否自动填充火药（直至满足当前蓝图打印所需的全部用量）。
- **GUI显示（适用于基础与高级）**：
  - 多个装填器连接时，打开任意一个均显示**合并总容量与总物品**，支持**滑动条**滚动查看。
- **完成提示（横幅）**：每张蓝图完成后弹一次。参照RS原版横幅样式：
  - 第一行：`已完成对 <蓝图名称> 资源的收集`
  - 第二行（全部足够）：`全部资源都已收集完毕。`
  - 第二行（有缺失，且**已安装自动合成升级**）：`其中 <物品名> 在网络中不存在或数量不足，也无法自动合成。`
  - 第二行（有缺失，且**未安装**自动合成升级）：仅提示数量不足，不提合成。
- **合成配方（工作台）**：
  - 顶行：空 | RS基础处理器 | 空
  - 中行：空 | 原版漏斗 | 空
  - 底行：空 | 原版箱子 | 空

---

## 3. 方块：高级蓝图加农炮装填器（高级版）
- **命名ID（拟定）**：`rs_create_compat:advanced_schematic_loader`
- **容量**：2个大箱子（108格）。
- **新增核心功能：蓝图打印列表（队列）**：
  - 内部拥有**蓝图队列存储区**，容量为1个箱子（27格，存放待打印的已部署蓝图）。
  - 点击开始后，**按顺序自动打印**队列中所有蓝图。
  - 流程：自动触发加农炮 → 自动获取资源 → 打印 → 打印完成后**自动回收空白蓝图** → 自动填入下一张蓝图 → 继续打印，直至队列清空。
- **继承基础版所有功能**（所有开关、协同、提示等）。
- **无加农炮时的逻辑**：必须等待当前蓝图的全部资源被取完，才能获取下一张蓝图的资源列表。
- **合成配方（工作台）**：
  - 顶行：空 | RS高级处理器 | 空
  - 中行：空 | 基础版装填器 | 空
  - 底行：空 | 机械动力：精密机构 | 空

---

## 4. 方块：序列装配样板终端
- **命名ID（拟定）**：`rs_create_compat:sequence_pattern_terminal`
- **核心目标**：将机械动力的序列装配接入RS自动合成体系，实现流水线批量生产。
- **硬性要求**：必须兼容**概率产物**和**废料（副产品）**。
- **基础组成：序列装配单元样板**（ID：`rs_create_compat:sequence_unit_pattern`）：
  - 代表一个具体操作步骤（冲压、磨砂、机械手装配等）。
  - UI布局：左侧（样板槽）| 中间（目标机器名称，滚轮选择）| 右侧（加工时原料输入槽，可留空）。
- **主终端组合界面**：
  - 左侧（单元样板库）：4格宽，容量108格，配滑动条和搜索框。
  - 中间/右侧（流程编排）：将单元样板不消耗拖入，旁有 +/- 按钮设定重复次数。
  - 底部：**序列装配样板槽**（ID：`rs_create_compat:sequence_assembly_pattern`）+ 整体循环次数输入槽。
- **左下角特殊区域**：
  - 原料槽（初始投入物品）。
  - 预期产物槽（支持多个，支持概率）。
  - 废料槽（支持概率）。
- **与RS/机械动力联动（“+”号转移）**：
  - 点击序列装配GUI的“+”号，自动将配方转移至本终端。
  - 自动创建不存在的单元样板，自动生成总样板。
  - 转移前检测空白样板是否足够，不足时通过Tooltip提示。
- **横幅提示**（每完成一个循环弹一次）：
  - 第一行：`已完成序列装配任务！`
  - 第二行：`目标产物：` 后跟物品图标及名称。
  - 后续：`预计得到：<物品名> x<数量>`（数量 = 投入原料总量 × 概率），`实际得到：<物品名> x<数量>`，最后标注产率（保留两位小数，如 `产率：87.50%`）。
- **执行逻辑与检测方案（已采纳）**：
  - 采用“**批次追踪 + 出口稳定检测 + 安全超时**”。
  - 监控网络中产物/废料增量，结合原料库存停止变动，判定批次结束。
  - 废料与原料重合时，自动扣除废料声明物品，不回传误判；超时后强制询问是否手动标记完成。
- **合成配方（工作台）**：
  - 顶行：空 | RS高级处理器 | 空
  - 中行：RS样板终端 | 机械动力：黄铜机壳 | 机械动力：黄铜机壳
  - 底行：空 | 铁块 | 空
- **注意**：`序列装配样板`和`序列装配单元样板`**均无工作台合成配方**，仅能通过本终端将RS原版样板转换/生成而来。

---

## 5. 物品：高级远程多功能终端
- **命名ID（拟定）**：`rs_create_compat:advanced_remote_terminal`
- **包含界面（可切换）**：合成终端、样板终端、自动合成仓管理器、自动合成仓监视器、序列装配样板终端。
- **电量容量**：默认10,000,000 FE（可配置）。
- **可配置项（config）**：电量容量、启用/禁用哪些界面。
- **合成配方（工作台）**：
  - 顶行：空 | 末影之眼 | 空
  - 中行：RS无线终端 | 钻石块 | RS高级处理器
  - 底行：空 | 末影之眼 | 空

---

## 6. 方块：范围充电器
- **命名ID（拟定）**：`rs_create_compat:range_charger`
- **核心功能**：给范围内所有**可充电方块或物品**充电（方块和物品是否启用可分别配置）。
- **范围**：默认50×50×50（GUI中调整），最小1×1×1，最大100×100×100。耗电随范围增大/减小而较快联动。
- **充电参数**（均在config中）：
  - 充电速率：默认20 FE/t。
  - 电量缓存：默认1,000,000 FE。
  - 一次性充电对象数：默认10个。
  - 方块/物品充电开关。
- **合成配方（工作台）**：
  - 顶行：RS控制器 | 红石块 | RS控制器
  - 中行：机械动力：大齿轮 | 铁块 | 机械动力：大齿轮
  - 底行：空 | 铁块 | 空

---

## 7. 方块/物品：定量保持器
- **命名ID（拟定）**：`rs_create_compat:quantity_keeper`
- **中文译名**：定量保持器（或数量保持器）。
- **核心功能**：标记一个对象（物品/流体/气体），数量不足时**自动触发RS自动合成**（需自动合成升级）；数量超出时可选择**自动销毁过量**部分。
- **激活**：必须标记对象后才亮起工作，否则不亮。每tick耗电10 FE。
- **插件槽位**：共6个，支持速度升级（加快销毁速度）和自动合成升级。
- **高级版候选功能**（待定）：
  - 允许放入RS硬盘直接作为大容量抽屉使用。
  - 允许一个Keeper同时控制多个不同对象。
- **合成配方（工作台）**：
  - 顶行：空 | RS探测器 | 空
  - 中行：机械动力：黄铜机壳 | 原版箱子 | 机械动力：黄铜机壳
  - 底行：空 | 红石比较器 | 空

---

## 8. 物品：通用储存磁盘
- **命名ID（拟定）**：`rs_create_compat:universal_storage_disk`
- **核心功能**：可同时存入**物品、流体、气体**任意类型。
- **容量换算**：1 bucket（1B）= 1000 mB = 1个物品位。
- **配置项（config）**：基础容量（以物品位为单位）、是否允许多类型混存。
- **合成配方（工作台）**：
  - 顶行：空 | RS高级存储元件 | 空
  - 中行：空 | 原版桶 | 空
  - 底行：空 | 末影珍珠 | 空

---

## 9. 调整一：自动合成仓的存储与输出改进
- **配置位置**：config（可开关）。
- **改进内容**：自动合成仓自身增加内部存储空间，可同时存物品、流体、气体；容量足够大（或可配置），确保单次合成产物能全部存入；玩家可从合成仓中分别提取物品和流体，单独处理。

---

## 10. 调整二：输入/输出总线支持属性过滤器
- **配置位置**：config（可开关）。
- **改进内容**：所有带过滤器的输入/输出总线，均支持**Tag过滤**（如 `#minecraft:stone`、`#minecraft:iron_ores`），方便批量匹配。

---

## 11. 方块：网络打包机
- **命名ID（拟定）**：`rs_create_compat:network_packager`
- **核心功能**：从RS网络中提取物品和流体，打包成机械动力的**包裹**，并支持**蛙港（Frogport）** 物流系统传输。
- **流体打包**：当前为**备选项**（开发难度高），默认关闭。
- **耗电**：待机5 FE/t；打包时按物品数量/流体mB额外计费（config配置）。
- **合成配方（工作台）**：
  - 顶行：空 | RS输出总线 | 空
  - 中行：空 | 机械动力：打包机 | 空
  - 底行：空 | 机械动力：黄铜机壳 | 空

---

## 12. 方块：自动序列装配舱（概念设计方案）
- **命名ID（拟定）**：`rs_create_compat:auto_sequenced_assembler`
- **核心目标**：专门执行序列装配样板，解决原版自动合成仓无法处理多面输出和中间产物流转的问题。
- **建议方案**：
  - GUI中为**每一步操作**分别指定一个**输出面**（上/下/左/右/前/后），参考通用机械（Mekanism）的配置界面。
  - 装配舱从网络取初始原料 → 从步骤1指定面输出到对应机器 → 中间产物通过输入总线返回装配舱 → 执行步骤2 → 从步骤2指定面输出 → … → 最终产物送回网络。
  - 配备**内部暂存区**存放中间产物，避免循环死锁（按批次ID匹配）。
- **配置项（config）**：暂存区容量（默认54格）、每tick最大提取/输出速率。
- **合成配方（工作台）**：
  - 顶行：空 | RS自动合成仓 | 空
  - 中行：机械动力：序列装配机 | 机械动力：黄铜机壳 | 机械动力：序列装配机
  - 底行：空 | 机械动力：精密机构 | 空

---

## 13. 全局配置归属原则（重申）
- **语言文件（.json）**：所有显示文字（横幅、按钮名称、提示语、Tooltip）。
- **配置文件（.toml）**：所有数值/开关（电量、速率、容量、开关、阈值等）。
- **GUI调整**：范围、开关按钮等直接在游戏内GUI调整的，不在config中重复。
- **未明确说明在哪里的，默认在config中配置**。

---

## 14. 所有合成配方汇总一览
| 方块/物品 | 配方（工作台，占用3x3） |
| :--- | :--- |
| 蓝图加农炮装填器（基础版） | 空  RS基础处理器  空 <br> 空  原版漏斗  空 <br> 空  原版箱子  空 |
| 高级蓝图加农炮装填器 | 空  RS高级处理器  空 <br> 空  基础版装填器  空 <br> 空  精密机构(机械动力)  空 |
| 序列装配样板终端 | 空  RS高级处理器  空 <br> RS样板终端  黄铜机壳(机械动力)  黄铜机壳(机械动力) <br> 空  铁块  空 |
| 高级远程多功能终端 | 空  末影之眼  空 <br> RS无线终端  钻石块  RS高级处理器 <br> 空  末影之眼  空 |
| 范围充电器 | RS控制器  红石块  RS控制器 <br> 大齿轮(机械动力)  铁块  大齿轮(机械动力) <br> 空  铁块  空 |
| 定量保持器 | 空  RS探测器  空 <br> 黄铜机壳(机械动力)  原版箱子  黄铜机壳(机械动力) <br> 空  红石比较器  空 |
| 通用储存磁盘 | 空  RS高级存储元件  空 <br> 空  原版桶  空 <br> 空  末影珍珠  空 |
| 网络打包机 | 空  RS输出总线  空 <br> 空  打包机(机械动力)  空 <br> 空  黄铜机壳(机械动力)  空 |
| 自动序列装配舱 | 空  RS自动合成仓  空 <br> 序列装配机(机械动力)  黄铜机壳(机械动力)  序列装配机(机械动力) <br> 空  精密机构(机械动力)  空 |
| **序列装配样板** | **无配方（仅由序列装配样板终端转换生成）** |
| **序列装配单元样板** | **无配方（仅由序列装配样板终端转换生成）** |