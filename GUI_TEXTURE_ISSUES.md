GUI 贴图问题报告（2026-08-30，像素级扫描，由 tools/audit_gui_textures.py / tools/verify_gui.py 自动比对生成）
================================================================================
前置素材：
  bg.png   尺寸 256x256，浅灰色底（内部≈RGB 198）+ 深灰色边框（≈RGB 55）；有效内容仅 0-165 行，165 以下透明
  slot.png 尺寸 256x256，左上 17x17 为单槽原型：顶边/左边深灰边框(55)，内部中灰(139)，右下亮白高光(255)
  槽位粘贴间距 = 18px（素材 17px + 1px 空隙），位置 = Menu addSlot 坐标
  对比基准：各 Screen 声明的 imageWidth/Height、Menu addSlot 的 (x,y,cols,rows)、Screen 按钮 bounds

检查规则：
  1. 图像尺寸必须与 Screen.imageWidth/Height 完全一致
  2. 整幅背景必须由 bg.png 上半部分 9-slice 拉伸铺满（内部亮度≈198），不得有透明洞
  3. 槽位背景单元（18px 间距）必须与 Menu 声明的坐标一一对应
  4. 所有空白区域（标题/标签/间距/按钮区）不得有槽位背景
  5. 按钮矩形（bounds）不得与槽位背景重叠

================================================================================
修复前的问题（已全部修复）
================================================================================
1. 【根因】slot.png 有效槽位是 17x17 而非 18x18——直接 crop(0,0,18,18) 会
   把右下 1px 白色高光行误认为槽位内容，导致粘贴偏移 1px（用户早前反馈过 ±1 偏移）。
   修复：SLOT_PX=17 裁剪 + SLOT_GAP=18 间距粘贴。
2. 【根因】bg.png 的 165 以下区域是透明的，旧 draw_inventory 用它拼背包区域
   会产生"槽位悬浮在透明洞上"的效果（用户反馈"slot 背景不在整个背景之中"）。
   修复：整幅背景统一由 bg.png 0-165 有效区域 9-slice 连续拉伸，不再拼接透明区。
3. 【布局冲突】高级装填器升级槽 2x3（x=124, y=8..62）与队列区（x=8..170, y=30..84）
   大面积重叠，升级槽被队列槽位覆盖。修复：GUI 加高到 446，升级槽改为独立一行横排
   (8,326) 6 格，背包(348) 快捷栏(406) 按钮(428) 依次下移，互不重叠。

================================================================================
修复后状态（verify_gui.py 验证，全部通过）
================================================================================
1. advanced_remote_terminal.png  176x120   控件面板，无物品槽      ✔ 无问题
2. range_charger.png             176x84    控件面板，无物品槽      ✔ 无问题
3. quantity_keeper.png           176x166   背景亮度 R=198 ✔
     标记槽(8,20) 1x1、插件槽(62,20) 2x3、背包(8,84) 9x3、快捷栏(8,142) 9x1
     按钮 -(128,20) +(150,20) 销毁(110,42) 无槽位冲突               ✔ 43/43 槽位
4. schematic_loader.png          208x222   背景亮度 R=198 ✔
     蓝图槽(8,8)、升级槽(35,8) 6 横排、存储(8,26) 9x6、
     背包(8,140) 9x3、快捷栏(8,198) 9x1                            ✔ 97/97 槽位
5. advanced_schematic_loader.png 176x446   背景亮度 R=198 ✔
     队列(8,30) 9x3、存储(8,98) 9x12、升级槽(8,326) 6 横排、
     背包(8,348) 9x3、快捷栏(8,406) 9x1
     按钮 A/D/R/G/Start @(8..100, 428) 无槽位冲突                  ✔ 177/177 槽位

================================================================================
坐标对照清单（make_gui_bg.py --slots 参数，必须是这些值，禁止栅格对齐）
================================================================================
★ schematic_loader.png (208x222):  --slots=8,8,1,1 --slots=35,8,6,1 --slots=8,26,9,6 --slots=8,198,9,1
                                    --with-inventory --slot-start-y 140 --slot-rows 3
★ advanced_schematic_loader.png (176x446):
                                    --slots=8,326,6,1 --slots=8,30,9,3 --slots=8,98,9,12 --slots=8,406,9,1
                                    --with-inventory --slot-start-y 348 --slot-rows 3
★ quantity_keeper.png (176x166):   --slots=8,20,1,1 --slots=62,20,2,3 --slots=8,142,9,1
                                    --with-inventory --slot-start-y 84 --slot-rows 3
★ range_charger.png (176x84):      无槽位
★ advanced_remote_terminal.png (176x120): 无物品槽（控件面板）
================================================================================
